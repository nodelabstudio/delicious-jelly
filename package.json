{
  "name": "delicious-jelly",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview --host 0.0.0.0",
    "deploy": "npm run build && npx --yes --package=node@22 --package=wrangler@4.129.0 wrangler deploy",
    "test": "node --import tsx --test src/*.test.ts"
  },
  "dependencies": {
    "three": "0.185.1"
  },
  "devDependencies": {
    "@types/node": "^22.18.0",
    "@types/three": "^0.183.1",
    "tsx": "^4.20.5",
    "typescript": "~5.9.3",
    "vite": "^6.4.1"
  }
}
